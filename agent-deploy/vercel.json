{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": null,
  "buildCommand": "rm -rf ./agent && cp -R ../agent ./agent && node node_modules/eve/bin/eve.js build",
  "ignoreCommand": "git diff --quiet HEAD^ HEAD -- ../agent ."
}
