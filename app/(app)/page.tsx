import { ChatExperience } from "@/app/_components/chat-experience";

// Homescreen: a fresh chat. The first completed turn navigates to /c/[id].
export default function HomePage() {
  return <ChatExperience />;
}
